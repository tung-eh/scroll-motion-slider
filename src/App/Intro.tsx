const Intro = () => {
  return (
    <section>
      <h1>
        Scroll to explore the rhythm of still images that move quietly between
        story and sentation.
      </h1>
    </section>
  )
}

export default Intro
